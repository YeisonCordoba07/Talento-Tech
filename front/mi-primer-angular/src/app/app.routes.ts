import { Routes } from '@angular/router';
import { UserComponent } from './view/user.component/user.component';

export const routes: Routes = [
    {
        path: 'users', component: UserComponent
    }
];
